import React from 'react';
import './Quote.scss';

export default function Quote (){
    return (
        <section className="quote">
            <p className="text">HARD WORK BEATS TALENT WHEN TALENT DOESN’T WORK HARD.</p>
        </section>
    );
}
